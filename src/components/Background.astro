<style>
  .marquee {
    width: 100%;
    height: 100%;
    color: rgb(0 0 0);
    font-size: 100vh;
    font-weight: bold;
    text-transform: uppercase;
    filter: blur(100px);
    mix-blend-mode: overlay;
    user-select: none;
  }

  .background {
    --first-color: #b8d8baff;
    --second-color: #d9dbbcff;
    --third-color: #fcddbcff;
    --fourth-color: #ef959dff;

    z-index: -1;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      -45deg,
      var(--first-color),
      var(--second-color),
      var(--third-color),
      var(--fourth-color)
    );
    background-size: 400% 400%;
    animation: gradient 15s ease infinite;
  }

  @keyframes gradient {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }
</style>

<div class="background">
  <marquee aria-hidden class="marquee">
    I'm so—I'm so reborn, I'm movin' forward 
    Keep movin' forward, keep movin' forward
  </marquee>
</div>
