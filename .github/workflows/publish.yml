name: publish

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
      - run: ls
      - run: npm ci
      - run: npm run build
    env:
      CI: true
